import { z } from "zod";import { readJson } from "@/lib/api-request";import { apiSuccess,getRequestId,handleApiError } from "@/lib/api-response";import { assertSameOrigin } from "@/server/auth/request";import { requireAdmin } from "@/server/admin/admin.service";import { replyAdminSupportTicket } from "@/server/productivity/admin-support.service";const schema=z.object({body:z.string().trim().min(1).max(15000)}).strict();type C={params:Promise<{id:string}>};export async function POST(request:Request,c:C){const rid=getRequestId(request);try{assertSameOrigin(request);await requireAdmin();const {id}=await c.params;const input=await readJson(request,schema);return apiSuccess({item:await replyAdminSupportTicket(id,input.body)});}catch(e){return handleApiError(e,rid)}}