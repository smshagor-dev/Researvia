import { NextRequest } from "next/server";import { apiSuccess,getRequestId,handleApiError } from "@/lib/api-response";import { searchPapers } from "@/server/research/research.service";export const runtime="nodejs";export async function GET(request:NextRequest){const requestId=getRequestId(request);try{const q=request.nextUrl.searchParams.get("q")??"";const limit=Number(request.nextUrl.searchParams.get("limit")??20);return apiSuccess({items:await searchPapers(q,Number.isFinite(limit)?limit:20)});}catch(error){return handleApiError(error,requestId)}}
