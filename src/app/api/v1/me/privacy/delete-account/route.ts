import { readJson } from "@/lib/api-request";import { apiSuccess,getRequestId,handleApiError } from "@/lib/api-response";import { deleteAccountSchema } from "@/schemas/privacy";import { assertSameOrigin } from "@/server/auth/request";import { clearSessionCookie,getCurrentUser } from "@/server/auth/session";import { AppError } from "@/server/errors/AppError";import { requestAccountDeletion } from "@/server/privacy/privacy.service";export const runtime="nodejs";export async function POST(request:Request){const requestId=getRequestId(request);try{assertSameOrigin(request);const user=await getCurrentUser();if(!user)throw new AppError("UNAUTHORIZED",401,"Authentication required.");const input=await readJson(request,deleteAccountSchema);const result=await requestAccountDeletion(user.id,input.password);const response=apiSuccess(result);clearSessionCookie(response);return response;}catch(error){return handleApiError(error,requestId)}}
