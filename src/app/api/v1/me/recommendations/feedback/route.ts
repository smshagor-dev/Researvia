import { readJson } from "@/lib/api-request";import { apiSuccess,getRequestId,handleApiError } from "@/lib/api-response";import { recommendationFeedbackSchema } from "@/schemas/expansion";import { assertSameOrigin } from "@/server/auth/request";import { getCurrentUser } from "@/server/auth/session";import { AppError } from "@/server/errors/AppError";import { saveRecommendationFeedback } from "@/server/workspace/workspace.service";export const runtime="nodejs";export async function POST(request:Request){const requestId=getRequestId(request);try{assertSameOrigin(request);const user=await getCurrentUser();if(!user)throw new AppError("UNAUTHORIZED",401,"Authentication required.");return apiSuccess({feedback:await saveRecommendationFeedback(user.id,await readJson(request,recommendationFeedbackSchema))});}catch(error){return handleApiError(error,requestId)}}
