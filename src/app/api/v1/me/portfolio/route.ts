import { readJson } from "@/lib/api-request";import { apiSuccess,getRequestId,handleApiError } from "@/lib/api-response";import { portfolioSchema } from "@/schemas/privacy";import { assertSameOrigin } from "@/server/auth/request";import { getCurrentUser } from "@/server/auth/session";import { AppError } from "@/server/errors/AppError";import { getOwnPortfolio,updateOwnPortfolio } from "@/server/portfolio/portfolio.service";export const runtime="nodejs";async function user(){const v=await getCurrentUser();if(!v)throw new AppError("UNAUTHORIZED",401,"Authentication required.");return v;}export async function GET(request:Request){const requestId=getRequestId(request);try{return apiSuccess({profile:await getOwnPortfolio((await user()).id)});}catch(error){return handleApiError(error,requestId)}}export async function PUT(request:Request){const requestId=getRequestId(request);try{assertSameOrigin(request);const current=await user();return apiSuccess({profile:await updateOwnPortfolio(current.id,await readJson(request,portfolioSchema))});}catch(error){return handleApiError(error,requestId)}}
