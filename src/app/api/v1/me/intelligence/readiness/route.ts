import { apiSuccess,getRequestId,handleApiError } from "@/lib/api-response";import { getCurrentUser } from "@/server/auth/session";import { AppError } from "@/server/errors/AppError";import { getApplicationReadiness } from "@/server/intelligence/intelligence.service";export const runtime="nodejs";export async function GET(request:Request){const requestId=getRequestId(request);try{const user=await getCurrentUser();if(!user)throw new AppError("UNAUTHORIZED",401,"Authentication required.");return apiSuccess(await getApplicationReadiness(user.id));}catch(error){return handleApiError(error,requestId)}}
