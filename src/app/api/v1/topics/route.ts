import { NextRequest } from "next/server";import { apiSuccess,getRequestId,handleApiError } from "@/lib/api-response";import { topicExplorer } from "@/server/research/research.service";export const runtime="nodejs";export async function GET(request:NextRequest){const requestId=getRequestId(request);try{return apiSuccess(await topicExplorer(request.nextUrl.searchParams.get("q")??""));}catch(error){return handleApiError(error,requestId)}}
