import { requireAdmin } from "@/server/admin/admin.service";
import { getDataQualitySummary } from "@/server/admin/platform.service";

export default async function DataQualityPage(){await requireAdmin();const data=await getDataQualitySummary();return <div className="space-y-6"><div><p className="text-sm font-medium text-slate-500">Research integrity operations</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Data Quality</h1><p className="mt-2 max-w-3xl text-sm text-slate-600">Published records are checked for unknown critical fields and verification staleness. Unknown values remain unknown instead of being inferred.</p></div><div className="rounded-xl border bg-white p-5"><p className="text-sm text-slate-600">Verification stale threshold: <strong>{data.staleThresholdDays} days</strong> · Data-change events in the last 30 days: <strong>{data.recentChanges}</strong></p></div><div className="grid gap-4 lg:grid-cols-2">{data.sections.map(section=><section key={section.key} className="rounded-xl border bg-white p-5"><div className="flex items-center justify-between"><h2 className="font-semibold">{section.label}</h2><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium">{section.total} published</span></div><div className="mt-4 space-y-2">{section.issues.map(issue=><div key={issue.label} className="flex items-center justify-between rounded-lg border px-3 py-2 text-sm"><span className="text-slate-600">{issue.label}</span><strong>{issue.count}</strong></div>)}</div></section>)}</div></div>}
