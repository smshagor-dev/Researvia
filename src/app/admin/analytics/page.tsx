import { requireAdmin } from "@/server/admin/admin.service";
import { getAnalyticsSummary } from "@/server/admin/platform.service";

function Ranking({title,items}:{title:string;items:Array<{label:string;count:number}>}){return <section className="rounded-xl border bg-white p-5"><h2 className="font-semibold">{title}</h2><div className="mt-4 space-y-2">{items.length?items.map(item=><div key={item.label} className="flex justify-between rounded-lg border px-3 py-2 text-sm"><span className="text-slate-600">{item.label.replaceAll("_"," ")}</span><strong>{item.count}</strong></div>):<p className="text-sm text-slate-500">No consented events recorded in this period.</p>}</div></section>}
export default async function AnalyticsPage(){await requireAdmin();const data=await getAnalyticsSummary(30);return <div className="space-y-6"><div><p className="text-sm font-medium text-slate-500">Privacy-aware product insight</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Analytics</h1><p className="mt-2 max-w-3xl text-sm text-slate-600">Aggregated event counts only. This dashboard does not expose raw private student documents, email content, or session tokens.</p></div><div className="rounded-xl border bg-white p-5"><p className="text-3xl font-semibold">{data.total}</p><p className="text-sm text-slate-500">events in the last {data.days} days</p></div><div className="grid gap-4 lg:grid-cols-3"><Ranking title="Activity" items={data.byType}/><Ranking title="Countries" items={data.byCountry}/><Ranking title="Fields" items={data.byField}/></div></div>}
