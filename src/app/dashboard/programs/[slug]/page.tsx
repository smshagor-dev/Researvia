import Link from "next/link";
import { notFound } from "next/navigation";
import { SaveButton } from "@/components/saved/SaveButton";
import { getProgramBySlug } from "@/server/discovery/discovery.service";
import { AppError } from "@/server/errors/AppError";
async function load(slug:string){try{return await getProgramBySlug(slug);}catch(error){if(error instanceof AppError&&error.status===404)notFound();throw error;}}
export default async function ProgramDetailPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const program=await load(slug);return <div className="mx-auto max-w-5xl"><Link href="/dashboard/programs" className="text-sm font-medium text-slate-500 hover:text-slate-950">← Back to programs</Link><div className="mt-5 rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-sm font-medium text-slate-500">{program.degreeLevel.replaceAll("_"," ")}</p><h1 className="mt-1 text-3xl font-semibold tracking-tight text-slate-950">{program.name}</h1><p className="mt-2 text-sm text-slate-600">{program.universityName}{program.departmentName?` • ${program.departmentName}`:""}</p></div><SaveButton itemType="PROGRAM" targetId={program.id}/></div><dl className="mt-7 grid gap-5 border-y border-slate-100 py-6 sm:grid-cols-3"><div><dt className="text-xs uppercase tracking-wide text-slate-400">Deadline</dt><dd className="mt-1 text-sm font-medium text-slate-900">{program.deadline?new Date(program.deadline).toLocaleDateString():"Not provided"}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-400">Intake</dt><dd className="mt-1 text-sm font-medium text-slate-900">{program.intake||"Not provided"}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-400">Funding</dt><dd className="mt-1 text-sm font-medium text-slate-900">{program.funding||"Not provided"}</dd></div></dl><p className="mt-6 text-sm leading-7 text-slate-600">{program.description||"No program description is available from the current source."}</p>{program.requirements.length?<section className="mt-6"><h2 className="font-semibold text-slate-950">Requirements</h2><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-slate-600">{program.requirements.map((item)=><li key={item}>{item}</li>)}</ul></section>:null}{program.researchAreas.length?<div className="mt-6 flex flex-wrap gap-2">{program.researchAreas.map((area)=><span key={area} className="rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-xs font-medium text-slate-700">{area}</span>)}</div>:null}<div className="mt-7 flex flex-wrap gap-3"><Link href={`/dashboard/applications?new=1&title=${encodeURIComponent(program.name)}&university=${encodeURIComponent(program.universityName)}${program.deadline?`&deadline=${encodeURIComponent(program.deadline)}`:""}`} className="rounded-lg bg-slate-950 px-4 py-2 text-sm font-medium text-white">Start application</Link>{program.universitySlug?<Link href={`/dashboard/universities/${program.universitySlug}`} className="rounded-lg border border-slate-200 px-4 py-2 text-sm font-medium text-slate-700">View university</Link>:null}{program.website?<a href={program.website} target="_blank" rel="noreferrer" className="rounded-lg border border-slate-200 px-4 py-2 text-sm font-medium text-slate-700">Official program page</a>:null}</div></div></div>}
