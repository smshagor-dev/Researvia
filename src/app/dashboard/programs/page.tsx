import Link from "next/link";
import { programSearchSchema } from "@/schemas/discovery";
import { searchPrograms } from "@/server/discovery/discovery.service";
export const metadata={title:"Programs | ResearVia"};
export default async function ProgramsPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const raw=await searchParams;const parsed=programSearchSchema.safeParse({q:typeof raw.q==="string"?raw.q:"",degreeLevel:typeof raw.degreeLevel==="string"?raw.degreeLevel:"",researchArea:typeof raw.researchArea==="string"?raw.researchArea:"",page:typeof raw.page==="string"?raw.page:1,limit:20});const query=parsed.success?parsed.data:{q:"",degreeLevel:"" as const,researchArea:"",universityId:undefined,departmentId:undefined,page:1,limit:20};const result=await searchPrograms(query);return <div className="mx-auto max-w-6xl"><div className="border-b border-slate-200 pb-6"><p className="text-sm font-medium text-slate-500">Academic discovery</p><h1 className="mt-1 text-3xl font-semibold tracking-tight text-slate-950">Degree & Research Programs</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">Browse published academic programs with deadlines, funding, requirements, departments and research focus.</p></div><form className="mt-6 grid gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm lg:grid-cols-[1fr_180px_220px_auto]"><input name="q" defaultValue={query.q} placeholder="Program or keyword" className="h-10 rounded-lg border border-slate-200 px-3 text-sm"/><select name="degreeLevel" defaultValue={query.degreeLevel} className="h-10 rounded-lg border border-slate-200 px-3 text-sm"><option value="">All levels</option><option value="MASTERS">Masters</option><option value="PHD">PhD</option><option value="POSTDOC">Postdoc</option><option value="BACHELORS">Bachelors</option><option value="CERTIFICATE">Certificate</option><option value="OTHER">Other</option></select><input name="researchArea" defaultValue={query.researchArea} placeholder="Research area" className="h-10 rounded-lg border border-slate-200 px-3 text-sm"/><button className="h-10 rounded-lg bg-slate-950 px-5 text-sm font-medium text-white">Search</button></form><div className="mt-6 text-sm text-slate-500">{result.total} published programs</div>{result.items.length?<div className="mt-6 grid gap-4 md:grid-cols-2">{result.items.map((program)=><Link key={program.id} href={`/dashboard/programs/${program.slug}`} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm hover:shadow-md"><div className="flex items-start justify-between gap-3"><h2 className="font-semibold text-slate-950">{program.name}</h2><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium text-slate-600">{program.degreeLevel.replaceAll("_"," ")}</span></div><p className="mt-1 text-sm text-slate-500">{program.universityName}{program.departmentName?` • ${program.departmentName}`:""}</p><p className="mt-3 line-clamp-2 text-sm leading-6 text-slate-600">{program.researchAreas.join(" • ")||program.description||"Program details not provided."}</p></Link>)}</div>:<div className="mt-6 rounded-xl border border-dashed border-slate-300 bg-white px-6 py-14 text-center text-sm text-slate-500">No published programs match these filters yet.</div>}</div>}
