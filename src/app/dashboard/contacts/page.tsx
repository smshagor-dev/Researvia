import { redirect } from "next/navigation";import { ContactsClient } from "@/components/productivity/ContactsClient";import { getCurrentUser } from "@/server/auth/session";export default async function Page(){if(!await getCurrentUser())redirect("/login");return <ContactsClient/>}