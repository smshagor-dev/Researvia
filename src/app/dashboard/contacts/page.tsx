import { redirect } from "next/navigation";import { ContactsClient } from "@/components/productivity/ContactsClient";import { getCurrentUser } from "@/server/auth/session";import { listAcademicContacts } from "@/server/productivity/productivity.service";export default async function Page(){const user=await getCurrentUser();if(!user)redirect("/login");const initialItems=JSON.parse(JSON.stringify(await listAcademicContacts(user.id)));return <ContactsClient initialItems={initialItems}/>}