import { redirect } from "next/navigation";import { PlannerClient } from "@/components/productivity/PlannerClient";import { getCurrentUser } from "@/server/auth/session";import { listPlannerTasks } from "@/server/productivity/productivity.service";export default async function Page(){const user=await getCurrentUser();if(!user)redirect("/login");const initialItems=JSON.parse(JSON.stringify(await listPlannerTasks(user.id)));return <PlannerClient initialItems={initialItems}/>}