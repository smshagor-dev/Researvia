import { redirect } from "next/navigation";import { PlannerClient } from "@/components/productivity/PlannerClient";import { getCurrentUser } from "@/server/auth/session";export default async function Page(){if(!await getCurrentUser())redirect("/login");return <PlannerClient/>}