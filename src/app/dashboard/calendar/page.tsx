import { redirect } from "next/navigation";import { CalendarClient } from "@/components/productivity/CalendarClient";import { getCurrentUser } from "@/server/auth/session";import { listCalendar } from "@/server/workspace/workspace.service";export default async function Page(){const user=await getCurrentUser();if(!user)redirect("/login");const initialItems=JSON.parse(JSON.stringify(await listCalendar(user.id)));return <CalendarClient initialItems={initialItems}/>}