import { redirect } from "next/navigation";import { RecommendationLettersClient } from "@/components/productivity/RecommendationLettersClient";import { getCurrentUser } from "@/server/auth/session";import { listRecommendationRequests } from "@/server/productivity/productivity.service";export default async function Page(){const user=await getCurrentUser();if(!user)redirect("/login");const initialItems=JSON.parse(JSON.stringify(await listRecommendationRequests(user.id)));return <RecommendationLettersClient initialItems={initialItems}/>}