import Link from "next/link";
import { notFound } from "next/navigation";
import { DeadlineBadge } from "@/components/discovery/DeadlineBadge";
import { SaveButton } from "@/components/saved/SaveButton";
import { AppError } from "@/server/errors/AppError";
import { getScholarshipBySlug, type DeadlineState } from "@/server/opportunities/opportunity.service";

async function loadScholarship(slug: string) {
  try { return await getScholarshipBySlug(slug); }
  catch (error) { if (error instanceof AppError && error.status === 404) notFound(); throw error; }
}

export default async function ScholarshipDetailPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const item = await loadScholarship(slug);
  return <div className="mx-auto max-w-5xl"><Link href="/dashboard/scholarships" className="text-sm font-medium text-slate-500">← Back to scholarships</Link><article className="mt-5 rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-medium text-slate-500">{item.provider} • {item.country}</p><h1 className="mt-1 text-3xl font-semibold tracking-tight text-slate-950">{item.name}</h1>{item.universityName ? <p className="mt-2 text-sm text-slate-600">{item.universityName}</p> : null}</div><div className="flex items-center gap-2"><DeadlineBadge state={item.deadlineState as DeadlineState} /><SaveButton itemType="SCHOLARSHIP" targetId={item.id} /></div></div><dl className="mt-7 grid gap-5 border-y border-slate-100 py-6 sm:grid-cols-3"><div><dt className="text-xs uppercase tracking-wide text-slate-400">Funding</dt><dd className="mt-1 text-sm font-semibold text-slate-900">{item.fundingType}</dd><dd className="text-sm text-slate-500">{item.fundingAmount || "Amount not provided"}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-400">Deadline</dt><dd className="mt-1 text-sm font-semibold text-slate-900">{item.deadline ? new Intl.DateTimeFormat("en", { dateStyle: "long" }).format(new Date(item.deadline)) : "Unknown"}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-400">Degree levels</dt><dd className="mt-1 text-sm text-slate-700">{item.degreeLevels.join(", ") || "Not provided"}</dd></div></dl><section className="mt-6"><h2 className="font-semibold text-slate-950">Eligibility</h2><p className="mt-2 whitespace-pre-line text-sm leading-7 text-slate-600">{item.eligibility || "Eligibility details were not provided by the current source."}</p></section><section className="mt-6 grid gap-5 sm:grid-cols-2"><div><h2 className="font-semibold text-slate-950">Coverage</h2><p className="mt-2 text-sm leading-6 text-slate-600">Tuition: {item.tuitionCoverage || "Not provided"}<br />Stipend: {item.stipend || "Not provided"}<br />Travel: {item.travelSupport || "Not provided"}</p></div><div><h2 className="font-semibold text-slate-950">Required documents</h2><p className="mt-2 text-sm leading-6 text-slate-600">{item.requiredDocuments.join(", ") || "Not provided"}</p></div></section><div className="mt-7 flex flex-wrap gap-3"><Link href={`/dashboard/applications/new?sourceType=SCHOLARSHIP&sourceId=${item.id}`} className="rounded-lg bg-slate-950 px-4 py-2 text-sm font-medium text-white">Track application</Link><a href={item.applicationUrl} target="_blank" rel="noreferrer" className="rounded-lg border border-slate-200 px-4 py-2 text-sm font-medium text-slate-700">Official application</a><a href={item.sourceUrl} target="_blank" rel="noreferrer" className="rounded-lg border border-slate-200 px-4 py-2 text-sm font-medium text-slate-700">Verify source</a>{item.universitySlug ? <Link href={`/dashboard/universities/${item.universitySlug}`} className="rounded-lg border border-slate-200 px-4 py-2 text-sm font-medium text-slate-700">View university</Link> : null}</div></article><div className="mt-5 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-900">Always confirm deadline, eligibility and funding on the official source before applying.</div></div>;
}
