import { redirect } from "next/navigation";import { WritingClient } from "@/components/productivity/WritingClient";import { getCurrentUser } from "@/server/auth/session";import { listWriting } from "@/server/workspace/workspace.service";export default async function Page(){const user=await getCurrentUser();if(!user)redirect("/login");const initialItems=JSON.parse(JSON.stringify(await listWriting(user.id)));return <WritingClient initialItems={initialItems}/>}