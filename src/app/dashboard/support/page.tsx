import { redirect } from "next/navigation";import { SupportClient } from "@/components/productivity/SupportClient";import { getCurrentUser } from "@/server/auth/session";import { listSupportTickets } from "@/server/productivity/productivity.service";export default async function Page(){const user=await getCurrentUser();if(!user)redirect("/login");const initialItems=JSON.parse(JSON.stringify(await listSupportTickets(user.id)));return <SupportClient initialItems={initialItems}/>}