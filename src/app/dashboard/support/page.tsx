import { redirect } from "next/navigation";import { SupportClient } from "@/components/productivity/SupportClient";import { getCurrentUser } from "@/server/auth/session";export default async function Page(){if(!await getCurrentUser())redirect("/login");return <SupportClient/>}