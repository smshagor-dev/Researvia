import { redirect } from "next/navigation";import { InterviewPrepClient } from "@/components/productivity/InterviewPrepClient";import { getCurrentUser } from "@/server/auth/session";import { listInterviewPrep } from "@/server/workspace/workspace.service";export default async function Page(){const user=await getCurrentUser();if(!user)redirect("/login");const initialItems=JSON.parse(JSON.stringify(await listInterviewPrep(user.id)));return <InterviewPrepClient initialItems={initialItems}/>}