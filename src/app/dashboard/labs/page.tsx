import Link from "next/link";
import { labSearchSchema } from "@/schemas/discovery";
import { searchLabs } from "@/server/discovery/discovery.service";
export const metadata={title:"Research Labs | ResearVia"};
export default async function LabsPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const raw=await searchParams;const parsed=labSearchSchema.safeParse({q:typeof raw.q==="string"?raw.q:"",researchArea:typeof raw.researchArea==="string"?raw.researchArea:"",page:typeof raw.page==="string"?raw.page:1,limit:20});const query=parsed.success?parsed.data:{q:"",researchArea:"",universityId:undefined,departmentId:undefined,page:1,limit:20};const result=await searchLabs(query);return <div className="mx-auto max-w-6xl"><div className="border-b border-slate-200 pb-6"><p className="text-sm font-medium text-slate-500">Academic discovery</p><h1 className="mt-1 text-3xl font-semibold tracking-tight text-slate-950">Research Labs & Groups</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">Discover published research labs, their focus areas, faculty, official contact channels and university relationships.</p></div><form className="mt-6 grid gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-[1fr_260px_auto]"><input name="q" defaultValue={query.q} placeholder="Lab or keyword" className="h-10 rounded-lg border border-slate-200 px-3 text-sm"/><input name="researchArea" defaultValue={query.researchArea} placeholder="Research area" className="h-10 rounded-lg border border-slate-200 px-3 text-sm"/><button className="h-10 rounded-lg bg-slate-950 px-5 text-sm font-medium text-white">Search</button></form><div className="mt-6 text-sm text-slate-500">{result.total} published labs</div>{result.items.length?<div className="mt-6 grid gap-4 md:grid-cols-2">{result.items.map((lab)=><Link key={lab.id} href={`/dashboard/labs/${lab.slug}`} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm hover:shadow-md"><h2 className="font-semibold text-slate-950">{lab.name}</h2><p className="mt-1 text-sm text-slate-500">{lab.universityName}{lab.departmentName?` • ${lab.departmentName}`:""}</p><p className="mt-3 line-clamp-2 text-sm leading-6 text-slate-600">{lab.researchAreas.join(" • ")||lab.description||"Research areas not provided."}</p></Link>)}</div>:<div className="mt-6 rounded-xl border border-dashed border-slate-300 bg-white px-6 py-14 text-center text-sm text-slate-500">No published research labs match these filters yet.</div>}</div>}
