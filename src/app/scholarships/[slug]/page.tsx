import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { AppError } from "@/server/errors/AppError";
import { getScholarshipBySlug } from "@/server/opportunities/opportunity.service";

type Props={params:Promise<{slug:string}>};async function load(slug:string){try{return await getScholarshipBySlug(slug);}catch(error){if(error instanceof AppError&&error.status===404)return null;throw error;}}
export async function generateMetadata({params}:Props):Promise<Metadata>{const {slug}=await params;const item=await load(slug);if(!item)return{title:"Scholarship not found | ResearVia",robots:{index:false,follow:false}};return{title:`${item.name} | Scholarship | ResearVia`,description:(item.eligibility||`${item.provider} scholarship in ${item.country}`).slice(0,160),alternates:{canonical:`/scholarships/${item.slug}`},robots:{index:true,follow:true}};}
export default async function ScholarshipPage({params}:Props){const {slug}=await params;const item=await load(slug);if(!item)notFound();const structured={"@context":"https://schema.org","@type":"Grant",name:item.name,funder:{"@type":"Organization",name:item.provider},areaServed:item.country,url:item.applicationUrl,description:item.eligibility||undefined};return <main className="mx-auto min-h-screen max-w-5xl px-6 py-12"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(structured).replace(/</g,"\\u003c")}}/><div className="flex items-center justify-between gap-4"><Link href="/scholarships" className="text-sm font-medium text-slate-600">← Scholarships</Link><Link href="/login" className="text-sm font-medium">Sign in to save or track</Link></div><article className="mt-8 rounded-2xl border bg-white p-7 shadow-sm"><p className="text-sm font-medium text-slate-500">{item.provider} · {item.country}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{item.name}</h1>{item.universityName?<p className="mt-2 text-sm text-slate-600">{item.universityName}</p>:null}<dl className="mt-7 grid gap-5 border-y py-6 sm:grid-cols-3"><div><dt className="text-xs uppercase tracking-wide text-slate-400">Funding</dt><dd className="mt-1 font-semibold">{item.fundingType}</dd><dd className="text-sm text-slate-500">{item.fundingAmount||"Amount not provided"}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-400">Deadline</dt><dd className="mt-1 font-semibold">{item.deadline?new Date(item.deadline).toLocaleDateString():"Unknown"}</dd><dd className="text-sm text-slate-500">{item.deadlineState.replaceAll("_"," ")}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-400">Degree levels</dt><dd className="mt-1 text-sm">{item.degreeLevels.join(", ")||"Not provided"}</dd></div></dl><section className="mt-6"><h2 className="font-semibold">Eligibility</h2><p className="mt-2 whitespace-pre-line text-sm leading-7 text-slate-700">{item.eligibility||"Eligibility details are not provided by the current source."}</p></section><section className="mt-6 grid gap-5 sm:grid-cols-2"><div><h2 className="font-semibold">Coverage</h2><p className="mt-2 text-sm leading-7 text-slate-700">Tuition: {item.tuitionCoverage||"Not provided"}<br/>Stipend: {item.stipend||"Not provided"}<br/>Travel: {item.travelSupport||"Not provided"}</p></div><div><h2 className="font-semibold">Required documents</h2><p className="mt-2 text-sm leading-7 text-slate-700">{item.requiredDocuments.join(", ")||"Not provided"}</p></div></section><div className="mt-7 flex flex-wrap gap-3"><a href={item.applicationUrl} target="_blank" rel="noreferrer" className="rounded-md bg-slate-950 px-4 py-2 text-sm font-medium text-white">Official application</a><a href={item.sourceUrl} target="_blank" rel="noreferrer" className="rounded-md border px-4 py-2 text-sm font-medium">Verify source</a>{item.universitySlug?<Link href={`/universities/${item.universitySlug}`} className="rounded-md border px-4 py-2 text-sm font-medium">University</Link>:null}</div><p className="mt-5 text-xs text-slate-500">Source: {item.source} · Last verified: {item.lastVerifiedAt?new Date(item.lastVerifiedAt).toLocaleDateString():"Not recorded"}</p></article><p className="mt-5 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">Always confirm funding, eligibility and deadline on the official source before applying.</p></main>}
